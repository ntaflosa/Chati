{
  "name": "chati",
  "version": "2.0.0",
  "private": true,
  "description": "Browser-based AI prompt generator",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.1"
  }
}
